const {
  activityMessages, botToken, clientID, guildID, steamcordToken,
// eslint-disable-next-line global-require
} = require('../config.json');

export {
  activityMessages, botToken, clientID, guildID, steamcordToken,
};
